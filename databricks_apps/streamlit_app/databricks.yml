# This is a Databricks asset bundle definition for stramlit_app.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: streamlit_app

include:
  - resources/*.yml

variables:
  sql_warehouse_id:
    default: c2c360ecde1dcaf8

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1942110704800933.13.azuredatabricks.net
  prod:
    mode: production
    workspace:
      host: https://adb-2909807607791205.5.azuredatabricks.net
      root_path: /Workspace/databricks_apps/${bundle.name}/${bundle.target}
    permissions:
      # - user_name: gabriel.rangel@databricks.com
      - service_principal_name: 6996565f-1303-4497-886d-25325113270c
        level: CAN_MANAGE
    variables:
      sql_warehouse_id:
        default: 06d41d80655cdc54
